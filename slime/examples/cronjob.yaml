cronJob:
  enabled: true
  schedule: "*/5 * * * *"
  restartPolicy: OnFailure
  jobTemplate:
    containers:
      - name: job
        image:
          repository: hello-world
          tag: latest
        imagePullPolicy: IfNotPresent
    restartPolicy: Never
