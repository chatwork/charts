{{- if .Values.datadog.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "burrow.fullname" . }}-dd-conf
  labels:
    {{- include "burrow.labels" . | nindent 4 }}
data:
  burrow.yaml: |
    init_config:
    instances:
    - burrow_uri: http://localhost:{{ .Values.service.port }}
      clusters:
        {{- toYaml .Values.datadog.checks_d.clusters | nindent 8 }}
      {{- if .Values.datadog.checks_d.tags }}
      tags:
        {{- toYaml .Values.datadog.checks_d.tags | nindent 8}}
      {{- end }}
{{- end }}
