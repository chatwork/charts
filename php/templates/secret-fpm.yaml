{{- $root := . -}}
{{- $fpm := .Values.fpm -}}
{{- if .Values.fpm.secrets }}
{{- range $name, $secret := .Values.fpm.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "php.fullname" $root }}-fpm-{{ $name | replace "." "-" }}
  labels:
    app: {{ template "php.name" $root }}
    chart: {{ template "php.chart" $root }}
    release: {{ $root.Release.Name }}
    heritage: {{ $root.Release.Service }}
  annotations:
    checksum/fpm-secret/{{ $name | replace "." "-" }}: {{ tpl (toYaml $secret) $root | sha256sum }}
{{- with $fpm.annotations }}
{{ toYaml . | indent 4 }}
{{- end }}
type: Opaque
data:
{{- range $k, $v := $secret }}
{{ $k }}: {{ tpl (print $v) $root | b64enc | quote }}
{{- end }}
{{- end }}
{{- end }}
