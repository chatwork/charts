{{- if .Values.busybox.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "php.fullname" . }}-busybox
  labels:
    app: {{ template "php.name" . }}
    chart: {{ template "php.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    checksum/busybox-secret: {{ .Values.busybox.secrets | toYaml | sha256sum }}
{{- with .Values.busybox.annotations }}
{{ toYaml . | indent 4 }}
{{- end }}
type: Opaque
data:
{{- range $k, $v := .Values.busybox.secrets }}
  {{ $k }}: {{ print $v | b64enc | quote }}
{{- end }}
{{- end }}
