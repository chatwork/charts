{{- if .Values.nginx.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "php.fullname" . }}-nginx
  labels:
    app: {{ template "php.name" . }}
    chart: {{ template "php.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    checksum/nginx-secret: {{ .Values.nginx.secrets | toYaml | sha256sum }}
{{- with .Values.nginx.annotations }}
{{ toYaml . | indent 4 }}
{{- end }}
type: Opaque
data:
{{- range $k, $v := .Values.nginx.secrets }}
  {{ $k }}: {{ print $v | b64enc | quote }}
{{- end }}
{{- end }}
